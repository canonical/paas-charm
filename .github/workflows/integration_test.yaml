name: Integration tests

on:
  pull_request:
  schedule:
    - cron: "0 15 * * SAT"

jobs:
  integration-test:
    permissions:
      contents: read
      packages: write
      actions: read
    uses: canonical/charm-ci/.github/workflows/integration-test.yml@9e2958d9a36b7248c8ba2ef509d526df32f48a7f # deferred artifacts
    secrets: inherit
    with:
      charmcraft-channel: latest/edge
      rockcraft-channel: latest/edge
  required_status_checks:
    name: Required Integration Test Status Checks
    runs-on: ubuntu-latest
    needs:
      - integration-test
    if: always() && !cancelled()
    timeout-minutes: 5
    steps:
      - name: Check integration-test status
        run: |
          RESULT="${{ needs.integration-test.result }}"
          if [ "$RESULT" = "success" ] || [ "$RESULT" = "skipped" ]; then
            exit 0
          else
            echo "integration-test failed with status: $RESULT"
            exit 1
          fi      
