on:
  pull_request:

concurrency:
  group: rtdworkflows-${{ github.ref }}
  cancel-in-progress: true

jobs:
  rtd-docs-checks:
    uses: canonical/operator-workflows/.github/workflows/docs_rtd.yaml@c9e78575cf199e07860da02d31d391dbccc68bf0
    secrets: inherit
    with:
      python-version: '3.12'
      enable-sphinx-python-dependency-build-checks: false
